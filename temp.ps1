 = Get-Printer;  = @(); foreach ( in ) {  = @(); try {  = Get-PrintConfiguration -PrinterName .Name -ErrorAction SilentlyContinue; if (.PrintCapabilitiesXML) { [xml] = .PrintCapabilitiesXML;  = new-object Xml.XmlNamespaceManager .NameTable; .AddNamespace('psf', 'http://schemas.microsoft.com/windows/2003/08/printing/printschemaframework');  = .SelectNodes('//psf:Feature[@name=''psk:PageMediaSize'']/psf:Option', ); foreach ( in ) {  += .name.Replace('psk:', '').Replace('ns0001:', '') } } } catch {};  += @{ Name = .Name; SupportedSizes =  } };  | ConvertTo-Json -Depth 3
